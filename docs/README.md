# docs/

Reference material your agent reads — schema docs, API references, domain
cheat-sheets, worked examples. Point `system_prompt.md` at the specific files;
the agent reads them (with the Read tool) when a question needs them.

This folder starts empty. Add `.md` files as your agent needs them.

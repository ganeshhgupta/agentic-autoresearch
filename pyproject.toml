[project]
name = "agent"
version = "0.1.0"
description = "An agent scaffolded from the Qure agent template."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.0",
    "httpx>=0.27",
    "ddgs>=9.0",
]
